@page
@model TennisWebsite.Pages.BookingPages.BookingModel
@{
    ViewData["Title"] = "Create booking";
}

<div class="container p-4 Container-Background">
    <form method="post" asp-page-handler="OnPostCreateAsync">


@*         <div class="mb-3">
            <label asp-for="@Model.player1" class="control-label"> Spiller 1 </label>
            <input asp-for="@Model.player1" class="form-control", placeholder="@HttpContext.Session.GetString("Username")" disabled readonly />
        </div>

        <div class="mb-3">
            <label asp-for="@Model.player2" class="control-label"> Spiller 2 </label>
            <input asp-for="@Model.player2" class="form-control", placeholder="Indtast venligst brugernavnet på din medspiller." />
        </div>

        <div class="mb-3">
            <label asp-for="@Model.booking.Court.Name" class="control-label"> Bane </label>
            <input asp-for="@Model.booking.Court.Name" class="form-control", placeholder="@Model.booking.Court.Name" disabled readonly />
        </div>

        <div class="mb-3">
            <label asp-for="@Model.booking.Start" class="control-label"> Start tid </label>
            <input asp-for="@Model.booking.Start" class="form-control", placeholder="@Model.booking.Start.ToString()" disabled readonly />
        </div>

        <div class="mb-3">
            <label asp-for="@Model.booking.End" class="control-label"> Slut tid </label>
            <input asp-for="@Model.booking.End" class="form-control", placeholder="@Model.booking.End" disabled readonly />
        </div>*@


        <button type="submit" class="btn btn-info">Opret booking</button>
    </form>
</div>
